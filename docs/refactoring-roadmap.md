# 🗺️ Project Roadmap

| Stage | Status | Goals | Note |
| :--- | :---: | :--- | :--- |
| **🎯 Phase 1: Foundation & Audit** | ✅ | [ ✓ ] Create architecture document</br>[ ✓ ] Audit existing codebase</br>[ ✓ ] Identify collection pipeline</li> | Base for refactoring |
| **🏗️ Phase 2: Abstraction Layer** | ✅ | [ ✓ ] Introduce router collector (orchestrator + provider interface)</br>[ ✓ ] Extend snapshot schema (event["network"]["router"], populated when non-empty)</br>[ ✓ ] Add WAN change detection</br>[ ✓ ] Add configuration for notification trigger (notifications.notify_on)</br>  | Detection logic is ready but has nothing real to compare yet - AutoDetectionProvider.collect() still returns {} |
| **🔌 Phase 3: Integrations & Features** | 🔄 | [ ✓ ] Introduce Router integrations (TplinkProvider)</br>[ ✓ ] Add WAN provider (TP-Link, via tplinkrouterc6u)</br>[ ✓ ] Add capability detection (CLI diagnostic output, not persisted)</br>[ ✓ ] Add email notification channel (SMTP `EmailNotifier`, replacing the placeholder)</br>[ ✓ ] Re-verify `TplinkRouterProvider.get_client()`/`authorize()`/`get_status()` against real AX72 hardware, on `tplinkrouterc6u` main post-merge</br>[ ✓ ] Remove local `TplinkRouterAX72` workaround, simplify `TplinkProvider` to plain auto-detection</br>[ ✓ ] Add MikroTik router provider</br> | Both notification channels confirmed end-to-end with real sends. AX72 re-verified against real hardware - `get_client()` actually resolves to `TplinkRouterSG`, not our merged class, and works fully end-to-end; local workaround removed as a result (see CHANGELOG). MikroTik provider added and confirmed against real hardware (RB951Ui-2nD) - static/bridge path verified live; the DHCP-client-WAN branch is implemented but unverified, no such device available to test against. Other vendors remain unimplemented and untestable without hardware - see CONTRIBUTING.md |
| **📝 Phase 4: Documentation & Community** | 🔄 | [ ✓ ] Update documentation</br>[ ✓ ] Add contributor guidelines</br> | README, CHANGELOG, and this roadmap kept current each session. `CONTRIBUTING.md` added, documenting the RouterProvider pattern and the real-hardware-verification workflow used for TP-Link and MikroTik, for anyone adding a new router vendor |
| **🚀 Phase 5: Deployment & Startup** | ✅ | [ ✓ ] Fix Windows Task Scheduler startup task (working-directory bug, logon-trigger scope)</br>[ ✓ ] Verify startup task end-to-end on a real machine</br>[ ✓ ] Open upstream PR to `tplinkrouterc6u` for the AX72 fix</br> | Startup task confirmed working end-to-end (elevated creation required - `/RU` needs admin rights on Windows even for the current user); logs/config land in the project root correctly after a real logon. **PR #193 merged** into `AlexandrErohin/TP-Link-Archer-C6U:main`. Maintainer made follow-up edits post-merge and asked for a fresh real-device confirmation via `TplinkRouterProvider.get_client()`/`authorize()`/`get_status()` from `main` - tracked as a new item under Phase 3, since it's really integration-testing work, not deployment. Windows-only for now; cross-platform (e.g. systemd/cron) scheduling is out of scope unless requested |