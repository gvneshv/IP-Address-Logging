# 🗺️ Project Roadmap

| Stage | Status | Goals | Note |
| :--- | :---: | :--- | :--- |
| **🎯 Phase 1: Foundation & Audit** | ✅ | [ ✓ ] Create architecture document</br>[ ✓ ] Audit existing codebase</br>[ ✓ ] Identify collection pipeline</li> | Base for refactoring |
| **🏗️ Phase 2: Abstraction Layer** | ✅ | [ ✓ ] Introduce router collector (orchestrator + provider interface)</br>[ ✓ ] Extend snapshot schema (event["network"]["router"], populated when non-empty)</br>[ ✓ ] Add WAN change detection</br>[ ✓ ] Add configuration for notification trigger (notifications.notify_on)</br>  | Detection logic is ready but has nothing real to compare yet - AutoDetectionProvider.collect() still returns {} |
| **🔌 Phase 3: Integrations & Features** | 🔄 | [ ✓ ] Introduce Router integrations (TplinkProvider)</br>[ ✓ ] Add WAN provider (TP-Link, via tplinkrouterc6u)</br>[ ✓ ] Add capability detection (CLI diagnostic output, not persisted)</br>[ ✓ ] Add email notification channel (SMTP `EmailNotifier`, replacing the placeholder)</br>[&emsp;] Add MikroTik router provider</br> | Both notification channels now confirmed end-to-end with real sends (Telegram and email). MikroTik hardware is now available for testing (used near the intercom/camera setup) - no longer blocked on hardware access. Other vendors remain unimplemented and untestable without hardware |
| **📝 Phase 4: Documentation & Community** | ⏳ | [&emsp;] Update documentation</br>[&emsp;] Add contributor guidelines</br> | Final |
| **🚀 Phase 5: Deployment & Startup** | ✅ | [ ✓ ] Fix Windows Task Scheduler startup task (working-directory bug, logon-trigger scope)</br>[ ✓ ] Verify startup task end-to-end on a real machine</br>[ ✓ ] Open upstream PR to `tplinkrouterc6u` for the AX72 fix</br> | Startup task confirmed working end-to-end (elevated creation required - `/RU` needs admin rights on Windows even for the current user); logs/config land in the project root correctly after a real logon. PR opened against `AlexandrErohin/TP-Link-Archer-C6U`, awaiting maintainer review. Windows-only for now; cross-platform (e.g. systemd/cron) scheduling is out of scope unless requested |